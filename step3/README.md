## Step 3 examples for control-flow-statements.ts
To compile the TypeScript file and execute the output file at one shot use command in terminal:
```ts-node control-flow-statements.ts``` or ```npm run ts:step3```